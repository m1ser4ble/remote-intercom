{
  "name": "remote-intercom",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:bundle": "npm run build && node build-bundle.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "ws": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/ws": "latest",
    "esbuild": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
