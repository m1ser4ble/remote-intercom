import { build } from "esbuild";

await build({
  entryPoints: ["src/index.ts"],
  bundle: true,
  platform: "node",
  target: "node20",
  format: "esm",
  outfile: "dist/remote-intercom-extension.mjs",
  packages: "bundle",
  external: [
    "@earendil-works/pi-ai",
    "@earendil-works/pi-coding-agent",
  ],
  banner: {
    js: "// Remote Intercom bundled pi extension. Generated by npm run build:bundle.",
  },
  sourcemap: false,
});
