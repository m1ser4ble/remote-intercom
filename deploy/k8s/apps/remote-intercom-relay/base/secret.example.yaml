apiVersion: v1
kind: Secret
metadata:
  name: remote-intercom-relay-secret
  namespace: remote-intercom
type: Opaque
stringData:
  # Generate with: openssl rand -hex 32
  relay-token-secret: replace-with-stable-random-secret
